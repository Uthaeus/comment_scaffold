<%= form_with(model: test_ad, local: true) do |form| %>
  <% if test_ad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(test_ad.errors.count, "error") %> prohibited this test_ad from being saved:</h2>

      <ul>
      <% test_ad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :test_ad_title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :test_ad_body %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_area :image, id: :test_ad_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
